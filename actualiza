#!/usr/bin/env bash
# Apply dotfiles to this machine. Git synchronization is deliberately separate.
set -euo pipefail
repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$repo_dir/scripts/actualiza.py" "$@"
